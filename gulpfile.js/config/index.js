var config = {}

config.source = "./src"
config.project = config.source + "/braden_marshall"
config.projectAssets = config.project + "/assets"
config.projectStatic = config.project + "/static"
config.projectCollectStatic = config.source + "/static"

module.exports = config
