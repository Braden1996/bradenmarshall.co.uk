var config = {}

config.source = "./src"
config.project = config.source + "/braden_marshall"
config.projectAssets = config.project + "/assets/base/"
config.projectStatic = config.project + "/static/base/"
config.projectCollectStatic = config.source + "/static"

module.exports = config
